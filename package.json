{
  "name": "spouse-points",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx serve . -p 8080",
    "test": "npx vitest run",
    "test:watch": "npx vitest",
    "test:e2e": "npx playwright test"
  },
  "devDependencies": {
    "vitest": "^3.0.0",
    "@playwright/test": "^1.50.0",
    "serve": "^14.2.0"
  }
}
